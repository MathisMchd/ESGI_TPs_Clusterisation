apiVersion: v1
kind: Service
metadata:
  name: web-nginx-svc
spec:
  selector:
    app: web-nginx
  ports:
    - protocol: TCP
      port: 80        # Port du Service
      targetPort: 80  # Port du conteneur
  type: ClusterIP